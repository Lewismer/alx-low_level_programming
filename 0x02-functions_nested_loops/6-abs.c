#include "main.h"

/**
 * _abs - return the absolute value of a number.
 * @n: The number of which the absolue value is calculated.
 *
 * Return: the absolute value of the number
 */
int _abs(int n)
{

	if (n > 0)

		return (n);
	else
		return (-n);

}
